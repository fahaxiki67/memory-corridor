__pycache__/
*.py[cod]
.venv/

# The local ledger can contain task text and should not be committed by accident.
# Remove this line if the project intentionally versions its notebook.
.context-guard/


# Build artifacts
build/
dist/
*.egg-info/

# 显式列出状态锁文件；.context-guard/ 已整体忽略，这里再列一次以防有人在仓库根手放同名文件。
state.lock
